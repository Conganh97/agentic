# Sprints

Used when unfinished tasks **> 5**. Smaller work skips this folder.

`python3 scripts/sprint.py [REQ-###]` · plan/close via `/scrum sprint`.
`/scrum run` only readies tasks whose `sprint:` matches the ACTIVE file.
