# Requirements

Input requirements for the agentic team. One file per requirement: `REQ-NNN-<short-name>.md`.

Each requirement should state: goal, scope, out of scope, acceptance criteria, constraints.
A task derived from a requirement cannot enter `READY` without acceptance criteria.
