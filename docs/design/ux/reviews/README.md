# UX / FE visual reviews (PQA)

One file per PQA visual review: `TASK-###-review-NN.md` (copy `templates/ux-review.md`).
`reviewer: PQA`. Required before SA may merge a task with `requires_uxui: true` /
`work_type: FRONTEND`. PQA also writes these when merging `work_type: UX_UI`.
